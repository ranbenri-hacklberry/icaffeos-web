import { useOrderPush } from '../hooks/useOrderPush';

const OrderPusher = () => {
    // Background order pusher disabled to enforce direct-to-server writes
    // useOrderPush();
    return null;
};

export default OrderPusher;
